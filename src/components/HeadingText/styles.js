import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    container: {
        marginTop: 32,
        display: 'flex',
        flexDirection: 'row',
        alignItems: 'center'
    },
    textLine: {
        height: 30,
        width: 5,
        borderRadius: 8,
        marginRight: 8
    },
    textStyle:{
        fontSize: 20,
        fontWeight: 'bold',
    }
})  