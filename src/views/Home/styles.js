import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    container: {
        padding:2,
        display: 'flex',
        flexDirection: 'row',
        justifyContent: 'flex-end'
    },
})